// Selectores DOM

const heading = document.querySelector(".heading");
console.log(heading);

console.log(heading.tagName); // este sirve para ver que etiqueta tiene
console.log(heading.textContent); // ver el contenido del texto
console.log(heading.classList); // ver las calsses que tiene

// querySelector solo trae hasta un elemento, trae el primero siempre
// querySelectorAll Te trae todos lo elementos
