// Condicionales
// ------------------------------------------------------------------------------------------------------------------------

const disponible = 4000;
const retirar = 200;

const auth = true;

if (auth) {
  // se cumple la condicion
  console.log("es hora de perrear");
} else {
  // no se cumple la condicion
  console.log("No hay suficiente saldo");
}

// Operadores de comparacion
// ------------------------------------------------------------------------------------------------------------------------
/**
 * > Mayor que
 * < Menor que
 * >= Mayor o igual que
 * <= Menor o igual que
 * == Igual que
 * === Igual que y del mismo tipo estricto
 * !== Diferente que
 * && Y
 * || O
 * ! Negacion
 * ? Operador ternario
 */
